<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.3</version>
        <relativePath/>
    </parent>

    <groupId>com.example</groupId>
    <artifactId>practice-root</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>practice-root</name>
    <description>Root POM for multi-module Spring Boot practice project</description>

    <modules>
        <module>common-lib</module>
        <module>project1</module>
        <module>project2</module>
        <module>project3</module>
    </modules>

    <properties>
        <java.version>25</java.version>
        <lombok.version>1.18.38</lombok.version>
        <!-- springdoc 3.x is the Spring Boot 4 / Spring Framework 7 line. 2.x targets Boot 3.x and
             fails at runtime on /v3/api-docs with
             NoSuchMethodError: ControllerAdviceBean.<init>(Object) as soon as any @ControllerAdvice
             bean is in the context, because Spring Framework 7 removed that constructor. -->
        <springdoc.version>3.1.0</springdoc.version>
        <liquibase.version>4.29.2</liquibase.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.example</groupId>
                <artifactId>common-lib</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-api</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
